// Defaults applied to every puzzle page (each src/puzzles/<folder>/index.md).
// Individual puzzles can override any of these in their own front matter.
export default {
  layout: "layouts/puzzle.njk",
  tags: ["puzzle"],
};
